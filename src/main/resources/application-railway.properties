# Railway Database Configuration
# Database URL is handled by DatabaseConfig.java to convert Railway's format

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Disable H2 console in production
spring.h2.console.enabled=false

# Server Configuration
server.port=${PORT:8080}
server.servlet.context-path=/
server.address=0.0.0.0

# Logging Configuration
logging.level.root=INFO
logging.level.com.nflpickem=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN

# Security Configuration - Disabled HTTP Basic auth, using custom auth
# spring.security.user.name=${ADMIN_USERNAME:admin}
# spring.security.user.password=${ADMIN_PASSWORD}

# CORS Configuration for Railway
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

# Actuator for health checks only - Disable all metrics
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
management.endpoints.web.base-path=/actuator
management.metrics.enable.all=false
management.metrics.export.simple.enabled=false

# Startup Configuration
spring.main.allow-bean-definition-overriding=true
spring.jpa.defer-datasource-initialization=true

# Exclude problematic auto-configurations
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.reactive.ReactiveSecurityAutoConfiguration,org.springframework.boot.actuate.autoconfigure.metrics.MetricsAutoConfiguration

# NFL Configuration
NFL_SEASON_WEEKS=18

# Odds API Configuration
ODDS_API_KEY=${ODDS_API_KEY:}
ODDS_API_BASE_URL=https://api.the-odds-api.com/v4
ODDS_UPDATE_INTERVAL_HOURS=6

# NFL Season Configuration (optional - for accurate week calculation)
# Format: YYYY-MM-DD (e.g., 2024-09-05 for 2024 season start)
NFL_SEASON_START_DATE="2025-09-04"
